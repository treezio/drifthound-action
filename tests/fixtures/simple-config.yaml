tool_versions:
  terraform: "1.6.0"

scopes:
  - name: "test-scope-1"
    project: "test-project"
    environment: "production"
    directory: "./terraform/core"
    tool: "terraform"
    slack_channel: "#alerts"
